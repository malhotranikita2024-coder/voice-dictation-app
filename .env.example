DEEPGRAM_API_KEY=
GROQ_API_KEY=

# Optional, dev-only. Enables transcript/usage history in MongoDB Atlas.
# Leave blank to run with no database (paste still works normally).
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<db>
MONGODB_URI=
